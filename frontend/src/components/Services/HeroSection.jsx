import HeroSectionContainer from "../common/HeroSection";

const heading = "Our Services";
const content =
  "Upmotion Tech delivers tailored digital solutions—from web development to SEO—to drive growth, efficiency, and impactful results for your business.";

const HeroSection = () => {
  return <HeroSectionContainer heading={heading} content={content} />;
};

export default HeroSection;
